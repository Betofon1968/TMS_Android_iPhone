# Web address of the TMS server (the TMS repository), without a trailing slash.
# Leave empty for local development: requests go to http://localhost:3001 through the dev server.
VITE_TMS_API_URL=https://tms-truck.onrender.com
